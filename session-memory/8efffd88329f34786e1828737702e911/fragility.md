# Critical Paths
_Files and modules that are fragile or have complex dependencies. Things that break easily. Areas needing special care. Known timing sensitivities._

- `tests/test_c8_mutation_killers.py` uses literal `old`/`new` substring substitution to mutate `hooks/_lib/learn_anti_pattern_mining.py`. Variable renames in `_passes_gate` / `_gate_clauses` (e.g. `rounds` → `review_rounds`) silently break the harness with `AssertionError: Mutation source string not found`. Lockstep update required — preventive: comment-pin near `_gate_clauses` warning the harness pins the variable name.
- `bash-write-guard.sh` PreToolUse hook blocks shell-redirection append (`>>`, `cat ... >>`) to `.jsonl` files including `learning/{project_hash}/observations.jsonl`. Reflect step 6a observation-capture MUST use Python `os.open(path, os.O_WRONLY|os.O_CREAT|os.O_APPEND)` + `os.write`. Allowlisted exception: `jq -c '...' >> file` (soak-flag in #102).
- `skills/build-implementation/SKILL.md` line 109 phrase `cap reduces from 5 to 3` is byte-pinned by `tests/test_build_step_2b_glob_unchanged.py::test_step_2b_glob_byte_identical`. Any future Step 2b paragraph that introduces a SECOND `cap=3` phrase MUST distinguish via a regex-anchored discriminator (e.g. requiring a unique env-var token in the matched paragraph), else substring assertions match both paragraphs ambiguously.
- The `architect` agent has Read/Grep/Glob/WebFetch/WebSearch only — NO Write tool. Plans + scratchpad entries are delivered as the architect's final assistant message; orchestrator persists verbatim to `pipeline-state/{task-id}/plan.md`. Spawning architect with a "Write the plan" instruction is a misroute.
- Slice B's producer-side `phases.patch_critic.persona_rejections` writer is missing — only `orchestrator/parallel-dispatch-details.md` line 623 documents it as instruction; no code in `hooks/observation-capture.sh`, `skills/pipeline/SKILL.md` Step 6/7c, or `skills/batch-pipeline/SKILL.md` populates it. PR #105 consumer is forward-compatible (synthetic-test-data only); follow-up slice must wire the producer for persona-tagged role tagging to fire on real pipelines.
