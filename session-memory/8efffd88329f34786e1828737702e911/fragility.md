# Critical Paths
_Files and modules that are fragile or have complex dependencies. Things that break easily. Areas needing special care. Known timing sensitivities._

- `tests/test_c8_mutation_killers.py` uses literal substring substitution on `hooks/_lib/learn_anti_pattern_mining.py`. Variable renames in `_passes_gate` / `_gate_clauses` silently break with `AssertionError: Mutation source string not found`. Comment-pin near `_gate_clauses`.
- `bash-write-guard.sh` blocks `>>` / `cat … >>` to `.jsonl` AND `.token` files. Use Python `os.open(path, O_WRONLY|O_CREAT|O_APPEND)` + `os.write` for observations.jsonl AND approval tokens (orchestrator `.md`-only allowlist excludes `.token`). Allowlisted exception: `jq -c '…' >> file`.
- `skills/build-implementation/SKILL.md` line 109 phrase `cap reduces from 5 to 3` byte-pinned by `tests/test_build_step_2b_glob_unchanged.py`. Future `cap=3` paragraphs need unique discriminators.
- `architect` agent has Read/Grep/Glob/WebFetch/WebSearch only — NO Write. Plans delivered as final assistant message; orchestrator persists to `pipeline-state/{task-id}/plan.md`.
- Producer for `phases.patch_critic.persona_rejections` still missing — only documented in `orchestrator/parallel-dispatch-details.md`; no code in observation-capture path populates it.
- **Long-running pipelines: main advances mid-flight.** Slice branches rooted at pipeline-start SHA show phantom deletions vs current `main`. Per-slice review must scope to slice commit (`git show <sha>`), NOT `git diff main`. Rebase-the-stack at Ship is the reliable pattern.
- **PDR-RTV is single-slice only.** T=2 × N=4 = 8 worktrees per slice; multi-slice plans blow `CLAUDE_BESTOFN_MAX_WORKTREES` (default 6). Re-route to standard multi-slice; log `fallback_reason: missing-meta`.
- **Internal Eval Gate (`/internal-eval`) uncallable from Ship-phase build agent** — skill is `context: fork`, build agent allowlist excludes Skill/Agent. Workaround: defer to post-merge orchestrator-run, verify specific case locally, document `EVAL_DEFERRED_SHIP_TIME` in PR body.
- **Pre-existing test failures on broader main** (codebase-map work): `test_pre_agent_allowlist`, `test_settings_registers_*`, `test_resolve-instincts`, `test_tool_timing_capture`, `test_verdict_catalog_audit`, `test_pipeline_state_cleanup::test_reflect_cleanup_removes_subdir_in_one_op`, `test_fix_engineer_routing`. Distinguish "pre-existing on base" from "regression introduced by PR" via stash-replay at base.
