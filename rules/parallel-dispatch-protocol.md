# Parallel Dispatch Protocol

This file is a stub. Content has moved to:

- `rules/_detail/parallel-dispatch-protocol.md` — hybrid dispatch model (subagent vs team), team phases (Plan Validation heavy/light, Build, Planning Agent, Best-of-N, Review, Final Gate), subagent phases, team lifecycle, resource bounds, teammate prompt template, checkpoint vocabulary, batch execution.

Detailed orchestrator dispatch procedure: `~/.claude/orchestrator/parallel-dispatch-details.md`.

Existing references to `rules/parallel-dispatch-protocol.md` continue to resolve here. New
references should point to `rules/_detail/parallel-dispatch-protocol.md`.
