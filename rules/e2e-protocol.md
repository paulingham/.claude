---
paths:
  - "maestro/**"
  - "**/*.tsx"
  - "**/app/**"
---

# E2E Protocol

This file is a stub. Content has moved to:

- `protocols/e2e-protocol.md` — full Maestro E2E trigger matrix, flow-to-file mapping, prerequisites, pass/fail criteria.

Frontmatter `paths:` is preserved on this stub for any hook/skill that parses it via `rules/e2e-protocol.md` (hook discovery is path-keyed, not content-keyed).

Existing references to `rules/e2e-protocol.md` continue to resolve here. New
references should point to `protocols/e2e-protocol.md`.
