# Thinking Defaults Protocol

This file is a stub. Content has moved to:

- `protocols/thinking-defaults.md` — full protocol (precedence rules, role defaults summary, hook behaviour Path B, environment variables, implementation files, xhigh allocation policy and boundary, forensic/source-field integration).

Source of truth for default selection of `effort` and `display` on Agent spawns. Applied by `pre-agent-thinking.sh` PreToolUse hook (currently log-only pending Agent input schema exposing `thinking`).

Existing references to `rules/thinking-defaults.md` continue to resolve here. New
references should point to `protocols/thinking-defaults.md`.
