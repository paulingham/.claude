# Agent Protocol

This file is a stub. Content has been split into:

- `rules/core.md` — load-bearing invariants always loaded by every spawn (orchestrator code ban, main-branch invariant, worktree + commit protocol)
- `rules/_detail/agent-protocol.md` — full protocol (worktree lifecycle, scratchpad, agent memory, fix-receiving rules, dynamic agents, resource bounds, per-agent tool scoping)

Existing references to `rules/agent-protocol.md` continue to resolve here. New
references should point to `rules/core.md` for invariants or
`rules/_detail/agent-protocol.md` for the full protocol.
