# Pipeline Protocol

This file is a stub. Content has moved to:

- `rules/_detail/pipeline-protocol.md` — full pipeline protocol (skills mandatory, Best-of-N Build dispatch, structured pipeline state with DUAL_PATH soak, pre-flight protocol, phase checklist, review protocol with in-cycle fix rule, async review, environment-dependent debugging loop, enforcement).

The phase-skipped/gate-bypassed iron law and in-cycle fix iron law live in `rules/core.md`.

Detailed orchestrator pipeline procedures: `~/.claude/orchestrator/pipeline-orchestration.md`.

Existing references to `rules/pipeline-protocol.md` continue to resolve here. New
references should point to `rules/_detail/pipeline-protocol.md`.
