# Reflection Protocol

This file is a stub. Content has moved to:

- `protocols/reflection-protocol.md` — full reflection protocol (when to reflect, reflection checklist, root cause analysis, identifying improvements across project conventions / global rules / agents / skills / README, autonomous intelligence steps 6a-6e: observation capture, auto-learn gate, model-efficiency check, session memory update, scratchpad cleanup, persistent harness state commit).

The observation-capture iron law (every pipeline produces an observation) lives in `rules/core.md`.

Existing references to `rules/reflection-protocol.md` continue to resolve here. New
references should point to `protocols/reflection-protocol.md`.
