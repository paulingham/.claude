ATDD GREEN — slice-a-pixel-diff-pump
Generated: 2026-05-12T23:58:00Z

=== Python tests (Tier 0 contract + Tier 1 Python-side unit) ===
Command: python3 -m unittest tests/contract/spec_visual_regression_schema.py tests/test_visual_diff.py -v

Result:
.............................
----------------------------------------------------------------------
Ran 29 tests in 0.054s
OK

All 29 tests pass:
- 18 Tier 0 contract assertions (tests/contract/spec_visual_regression_schema.py)
- 11 Tier 1 Python-side unit assertions (tests/test_visual_diff.py)

=== JS tests (Tier 1.5 PBT-as-Tier-1-regressions for visual_diff.js) ===
Command: node --test tests/test_visual_diff.js

Result:
ℹ tests 9
ℹ suites 0
ℹ pass 9
ℹ fail 0
ℹ duration_ms ~31

All 9 JS tests pass:
- idempotence (identical buffers → 0.0)
- symmetry (diff(a,b) === diff(b,a))
- range invariant (ratio ∈ [0.0, 1.0])
- maximally-different (ratio = 1.0)
- half-different (~0.5 with anti-aliasing tolerance)
- size-mismatch rejection
- non-Buffer rejection
- threshold range validation
- comparePngBuffers helper return shape

Production code touched (cohesion rules continuously enforced — CC ≤ 5, nesting ≤ 2):
- NEW hooks/_lib/visual_diff.js (~140 LOC)
  Public API: computePixelDiffRatio(baseline, current, threshold, dims) -> number
              comparePngBuffers(baseline, current, opts) -> {ratio, diffPixelCount, totalPixels}
  Internal helpers: _assertBuffer, _assertThreshold, _assertDimensions,
                    _pixelDistance, _blend, _rgbToY, _rgbToI, _rgbToQ
  Algorithm: YIQ-distance pixel comparison (same shape as pixelmatch, AA gating
             deferred to Playwright's toHaveScreenshot layer).
- NEW hooks/_lib/baseline_capture.sh (~120 LOC)
  Public function: capture_baseline (entry point)
  Helpers: emit_auto_blessed_baseline, _create_baseline_worktree,
           _run_baseline_build, _cleanup_baseline_worktree,
           _emit_scratchpad_warning, _emit_index_captured_false,
           _emit_index_captured_true
  Iron Law 4 compliance: every git mutation uses `git -C "$REPO_ROOT"` delegation;
                        no bare `git checkout` or `git switch`.
- MODIFY skills/design-qc/SKILL.md
  - When-to-Invoke: extended frontend-touching glob to include `.html` (AC5).
  - Step 2 (npm install): replaced `puppeteer` with `@playwright/test` (AC2 port).
  - NEW Step 5.5 "Capture Baseline" (~30 lines, AC1 + AC6 + failure-mode-1).
  - Step 6: full Puppeteer → Playwright port. Documents toHaveScreenshot,
            testConfig.snapshotDir = '.claude/screenshots' override
            (failure-mode-9 / SE-5 path-contract preservation), failure-mode-2
            playwright-null-diff fragility token.
  - Step 6.25 index.json: schema_version 1 → 2 + new visual_regression block
            documentation with JSON example (per-route pixel_diff_ratio,
            baseline_path, current_path).
  - NEW "## Visual Regression" section near end: AC7 project CLAUDE.md schema
            (`default_max_diff_pixel_ratio`, `per_route` map, precedence
            semantics, backward-compat absent-section semantics, malformed-YAML
            failure-mode-6 fallback).

ATDD discipline verified:
- Batched RED captured BEFORE any production code was written. Audit artifact:
  pipeline-state/workstreams/visual-regression/design-qc-visual-regression/build-red-batched.txt
- All 29 Python tests + 9 JS tests RED at batched-RED time for the right reason
  (named behavior absent, no syntax errors, no missing imports).
- Implementation produced cohesive, well-named code on first pass — no separate
  refactor step (May 2026 ATDD policy).
- Cohesion rules enforced continuously during GREEN, not in a separate cleanup pass.

PROCEED TO MUTATION GATE.

================================================================================

ROUND 1 FIX-CYCLE — GREEN phase
Generated: 2026-05-12T(round-1 fix-cycle)

=== Python tests (all tiers) ===
Command: python3 -m unittest tests.test_visual_diff tests.contract.spec_visual_regression_schema tests.integration.test_design_qc_visual_regression_e2e -v

Tier 0 contract + Tier 1 Python-side unit:
  Ran 29 tests in 0.158s — OK
Tier 2 integration (real Playwright + npm + git worktree):
  Ran 1 test in 5.562s — OK (full E2E; skipped automatically when
  CLAUDE_SKIP_HEAVY_INTEGRATION=1 or node/npm absent)

=== JS tests (Tier 1 + Tier 1.5 PBT regressions) ===
Command: node --test tests/test_visual_diff.js
  Ran 21 tests — OK (no fail, no skip)

=== Tier 3 mutation gate (re-run via reproducible runner) ===
Command: bash tests/mutation/visual_diff_mutation_runner.sh
  15 mutants applied successfully
  11 KILLED, 4 SURVIVED (all 4 in documented-equivalent set {M4, M8, M10, M14})
  Raw kill rate:       0.733
  Effective kill rate: 1.000
  Gate (≥ 0.70):       PASS (raw clears; effective clears trivially)

Round-1 fix-cycle changes (round-1 of max 2 review rounds):

New artifacts:
- tests/integration/test_design_qc_visual_regression_e2e.py
  Tier 2 integration test exercising baseline_capture.sh + Playwright
  + visual_diff.js end-to-end against a fixture project, asserting the
  four contracted artifacts (a)-(d). Skips cleanly when node/npm absent
  or CLAUDE_SKIP_HEAVY_INTEGRATION=1.
- tests/fixtures/visual-regression-next/
  Fixture project: package.json (Playwright devDep), server.js (static
  HTTP server on port 4321), app/page.tsx + app/layout.tsx +
  next.config.js (Next.js shape per plan § 6), playwright-driver.js
  (Step 6 Playwright pump shim), README.md.
- tests/mutation/visual_diff_mutation_runner.sh
  Reproducible mutation-test runner committed to tree (no longer in
  gitignored .claude-scratch-tools/). 15 perl-based mutants, exit-1 on
  unexpected survivors or apply failures. Re-runs deterministically
  against current source.

Modified artifacts:
- tests/test_visual_diff.js
  Test docstring corrected for `partial-alpha pixels correctly` — was
  claiming M8 discrimination but empirical verification shows M8 mutant
  also satisfies the `> 0.5` assertion. Points readers to build-mutation
  .md § M8 for the equivalent-mutant rationale.
  Misleading test name `discriminates Y-luminance via primary-channel
  test` renamed to `red vs black yields ratio 1.0 (documents M7
  equivalent under pure-color inputs)` so the name matches what the
  docstring already admits.
- build-mutation.md
  Round-1 addendum documenting (1) the tests/mutation/ runner location
  and rationale (no top-level package.json → Option A Stryker
  impractical, Option B per skills/verify/SKILL.md manual fallback),
  (2) M8 empirical re-verification (both correct + mutant return
  ratio=1.0 for the partial-alpha test inputs; classification unchanged).

PROCEED TO ROUND-1 CODE-REVIEWER RE-DISPATCH.
