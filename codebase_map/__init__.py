"""Auto-generated codebase map: tree-sitter symbol extraction + PageRank.

Slice A scope: tag extraction core + SQLite cache + worktree-aware walk.
Slice B-F build on top (graph, hooks, resolver, soak, integration).

See `pipeline-state/auto-codebase-map/plan.md` for the full slice plan.
"""
